# 个人中心开发记录

## 适用范围
- 功能：个人中心资料维护、修改密码、相关接口设计与验收。
- 相关文件：
  - `vue-vben-admin/apps/web-ele/src/views/_core/profile/*`
  - `vue-vben-admin/apps/web-ele/src/api/core/user.ts`
  - `backend/src/routes/user.ts`
  - `backend/API_CONTRACT.md`

## 当前状态
- 个人中心已收敛为两个 Tab：`基本设置`、`修改密码`
- 已支持资料更新与修改密码
- 修改密码成功后会强制退出重新登录

## 目录
- `2026-03-07 v1.0`：个人中心功能补齐
- `2026-03-15 v1.1`：个人中心路由收敛

---

## 2026-03-07 v1.0

### 目标
- 将个人中心从纯展示页补齐为可用功能
- 合并原“安全设置”内容到“基本设置”
- 最终仅保留两个 Tab：`基本设置`、`修改密码`

### 细节优化记录
- 基本设置 UI：移除“账户安全/账户密码”独立块
- 手机并入表单，用只读输入框脱敏展示
- `username` 作为账号标识保持只读，避免影响历史数据一致性

### 现状梳理
- 路由：`/profile`
- 页面入口：`vue-vben-admin/apps/web-ele/src/views/_core/profile/index.vue`
- 基本设置：`base-setting.vue`
- 修改密码：`password-setting.vue`
- 历史 `security-setting.vue` 文件仍在，但不再作为独立 Tab 引用

### 已有接口
- `GET /api/user/info`
- `PUT /api/user/profile`
- `PUT /api/user/password`

### 页面结构决策
- `基本设置`：资料表单 + 账户安全展示信息
- `修改密码`：旧密码、新密码、确认密码
- 角色始终只读，由管理员在“用户管理”中修改

### 修改密码规则
- `oldPassword` 必填
- `newPassword` 必填，且不能与 `oldPassword` 相同
- `confirmPassword` 必填，且必须与 `newPassword` 相同

### UI/交互
- 手机脱敏展示；无手机号时显示“未绑定手机号”
- 修改密码成功后提示成功，并强制退出到登录页

### 接口设计
#### `PUT /api/user/profile`
- 入参：
  - `realName`
  - `introduction?`
  - `avatar?`
- 约束：
  - `realName` 不能为空
  - `introduction` 可为空
  - `avatar` 可为空

#### `PUT /api/user/password`
- 入参：
  - `oldPassword`
  - `newPassword`
- 约束：
  - 新旧密码不能相同
  - 旧密码错误返回失败
- 会话策略：
  - 成功后删除该用户所有 session，强制重新登录

### 前端改动点
- `index.vue`：仅保留两个 Tab
- `base-setting.vue`：接入资料保存
- `password-setting.vue`：接入修改密码与前端校验
- `src/api/core/user.ts`：新增个人中心相关 API

### 后端改动点
- `UserDoc` 增加：
  - `real_name?`
  - `introduction?`
- `GET /api/user/info` 增加对应字段映射
- 在 `backend/src/routes/user.ts` 新增资料更新、修改密码接口
- 在 `backend/API_CONTRACT.md` 补齐契约说明

### 验收清单
- 页面可正确回填 `realName/username/roles/introduction`
- 更新资料后能保存并刷新用户信息
- 手机脱敏展示正确
- 三项密码校验生效
- 旧密码错误能正确提示
- 修改密码成功后需重新登录

## 2026-03-15 v1.1

### 目标
- 将用户侧个人中心正式路由统一收敛为 `/profile`
- 消除右上角入口与左侧菜单进入后产生两个 Tab 的问题
- 保持个人中心页面内容与现有资料维护能力不变

### 范围
- 前端路由收敛：删除 `/user-center`
- 左侧菜单“个人中心”改为直接使用 `/profile`
- 右上角个人中心入口继续使用 `/profile`
- 同步更新项目说明与后端菜单示例中的路由路径

### 页面/路由改动点
- 保留正式路由：
  - `/profile`
- 删除重复路由：
  - `/user-center`
- 个人中心页面仍复用：
  - `vue-vben-admin/apps/web-ele/src/views/_core/profile/index.vue`

### 兼容性说明
- 本次不保留 `/user-center` 重定向兼容
- 历史手工输入地址、旧书签或外部直链若仍使用 `/user-center`，将不再可用

### 验收点
- 用户从左侧菜单进入个人中心时，只打开一个 `个人中心` Tab
- 用户从右上角头像菜单进入个人中心时，只打开同一个 `个人中心` Tab
- 进入 `/profile` 后，左侧“个人中心”菜单处于激活状态
- `基本设置`、`修改密码` 两个 Tab 功能无回归

## 2026-03-15 v1.2

### 目标
- 修正右上角用户下拉与个人中心中的展示信息
- 统一个人中心可编辑字段范围
- 为后续真实三角色改造预留明确的 UI 约束

### 范围
- 右上角用户下拉
- 个人中心 `基本设置`
- 个人中心字段文案与回填逻辑

### 页面改动点
- 右上角下拉副标题：
  - 从固定邮箱改为当前登录用户手机号
- 右上角角色标签：
  - 从固定 `Pro` 改为当前角色标签
  - 标签文案：
    - `user -> reader`
    - `admin -> admin`
    - `super -> super`
  - 标签样式需与“用户管理”页角色标签保持一致
- 个人中心手机号展示：
  - 取消脱敏，显示完整手机号
- 个人中心字段文案：
  - `姓名` 统一改为 `昵称`
  - 接口字段名仍保持 `realName`，本次仅调整展示文案，不改接口契约

### 可编辑范围
- `user` 可修改：
  - `昵称`
  - `个人简介`
  - `手机号`
- `admin` 可修改：
  - `昵称`
  - `个人简介`
  - `手机号`
- `super` 可修改：
  - `昵称`
  - `个人简介`
  - `手机号`
- 所有角色均保持只读：
  - `用户名`
  - `角色`

### 接口影响
- `PUT /api/user/profile`
  - 继续沿用现有入参：
    - `realName`
    - `introduction?`
    - `avatar?`
  - 后续需补充手机号更新能力，以支撑个人中心自助修改手机号

### 验收点
- 右上角下拉展示当前手机号，不再显示固定邮箱
- 右上角角色标签与当前登录角色一致，且样式与用户管理页一致
- 个人中心手机号完整展示，不再脱敏
- 个人中心“昵称/个人简介/手机号”可编辑并正确回填
- `用户名/角色` 在个人中心中保持只读

### 本次实现补充
- 右上角用户下拉：
  - 副标题已改为当前登录用户手机号
  - 角色标签已改为真实角色标签：`super / admin / reader`
  - 标签样式改为与用户管理页一致的 `ElTag` 视觉
- 个人中心 `基本设置`：
  - `姓名` 已改为 `昵称`
  - `手机号` 已改为可编辑输入框，并取消脱敏显示
  - `用户名`、`角色` 继续保持只读
- 接口：
  - `PUT /api/user/profile` 已补充 `phone` 更新能力
  - 后端已增加手机号格式校验与唯一性冲突返回

### 自检记录
- `2026-03-15`
- `pnpm -C backend build`：通过
- `pnpm -C vue-vben-admin -F @vben/web-ele typecheck`：通过
- Windows PowerShell 冒烟：通过
  - `GET /api/health`：`200`
  - `POST /api/auth/login`：
    - `admin -> roles=admin`
    - `vben -> roles=super`
    - 临时用户 -> `roles=user`
  - `PUT /api/user/profile`：
    - 临时用户成功更新 `realName / introduction / phone`
    - 返回手机号为完整值，未脱敏
  - 本次临时账号：
    - `smoke_u_16534435`
