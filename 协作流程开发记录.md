# 协作流程开发记录

## 2026-03-15 v1.0

### 范围

- 目标：补强 `AGENTS.md` 中与分支管理、开发收尾相关的协作规则，减少误在 `main`/`master` 上直接修改的风险。
- 本次仅调整协作规范文档，不涉及前后端业务代码、接口契约、页面逻辑与运行脚本。

### 拟执行改动

- 修正 `AGENTS.md` 中表格页示例代码块的语言标记，避免 IDE 将 Markdown 示例按真实 Vue 文件解析并报错。
- 新增或强化“任何非只读操作前先检查当前分支”的规则。
- 新增或强化“若当前分支为 `main`/`master`，默认禁止直接修改代码/文档/删除文件/提交”的规则。
- 明确只有在用户显式要求时，才允许直接在 `main`/`master` 上执行写操作。

### 接口/页面改动点

- 接口：无。
- 页面：无。
- 路由：无。

### 验收点

- `AGENTS.md` 中相关规则表述从建议提升为硬约束。
- `AGENTS.md` 中表格页示例不再被 IDE 误判为可执行 Vue 文件而产生无关解析报错。
- 本次变更仅落在文档范围内。
