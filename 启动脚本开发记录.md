# 启动脚本开发记录

## 适用范围
- 功能：`start-dev.ps1` 的多 tab 启动逻辑、Codex tab 启动方式、外观一致性。
- 相关文件：`start-dev.ps1`

## 当前状态
- 当前脚本会启动数据库、Redis、后端、前端、codex 五个 tab。
- codex tab 当前以 `pwsh` 为入口，再进入 WSL 启动 `codex`。
- 该实现兼顾了外观一致性与退出后保留 WSL shell 的需求。

## 目录
- `2026-03-14 v1.0`：去掉 Codex 重复定位
- `2026-03-14 v1.1`：Codex tab 启动逻辑修复
- `2026-03-15 v1.2`：Codex tab 外观一致性调整
- `2026-03-16 v1.3`：Codex tab 改为 Windows 直启

---

## 2026-03-14 v1.0

### 背景
- `start-dev.ps1` 中 `codex` tab 已通过 WSL 进入项目目录，但又额外使用 `codex -C <wslRoot>`，存在重复定位。

### 目标
- 仅去掉 WSL/Codex 的重复定位，保持行为不变。

### 改动点
- 保留 `wsl.exe --cd <wslRoot>`
- 将 `exec codex -C '<wslRoot>'` 简化为 `exec codex`

### 验收点
- `codex` tab 进入项目目录后直接启动 `codex`
- 不再额外使用 `-C`

### 变更记录
- 已移除 `codex -C '<wslRoot>'`
- 当时一度将 tab 标题调整为 `wsl: codex`

---

## 2026-03-14 v1.1

### 范围
- 仅调整 `start-dev.ps1` 中 codex tab 的启动逻辑。

### 现状问题
- 早期方案由 `pwsh` 再调 `wsl.exe ... bash -lc '... exec codex'`
- 会引出几个问题：
  - tab 退出后回到 PowerShell，而不是留在 WSL
  - `bash -lc` 初始化链路与手动进入 WSL 可能不一致
  - `wt` 参数中的 `;` 容易被错误拆分

### 目标
- 让 codex tab 先进入 WSL，再在 WSL 内启动 `codex`
- `codex` 退出后保留一个可交互的 WSL shell

### 当时方案记录
- 计划改为直接以 `wsl.exe` 作为 tab 入口
- 依赖 `--startingDirectory` 落到项目目录
- 为 WSL 发行版预留可配置能力

### 冒烟记录
- `.\start-dev.ps1 -DryRun`
- `.\start-dev.ps1`
- 当时验证结果：
  - 可进入项目目录并启动 `codex`
  - `codex` 退出后保留交互式 WSL shell

### 备注
- 本方案是阶段性方案记录；后续被 `2026-03-15 v1.2` 的“PowerShell 入口 + 进入 WSL”方案覆盖。

---

## 2026-03-15 v1.2

### 范围
- 仅调整 codex tab 的启动方式与外观一致性。

### 现状问题
- 直接以 `wsl.exe` 作为 tab 入口时，Windows Terminal 会套用默认 WSL profile，导致字体/字号与其它 PowerShell tab 不一致。

### 目标
- 保持 codex tab 外观与 PowerShell tab 一致
- 同时仍在 WSL 中启动 `codex`
- `codex` 退出后仍保留可交互 WSL shell；退出 WSL 后回到 PowerShell

### 最终方案
- `wt new-tab ... pwsh -NoExit -Command <codexCmd>`
- `codexCmd` 内执行：
  - `wsl.exe -d Ubuntu -- bash -ilc '(codex || true) && exec bash -il'`

### 验收点
- 运行 `.\start-dev.ps1`
- codex tab 字体/字号与其它 PowerShell tab 一致
- 自动进入 WSL 并启动 `codex`
- `codex` 退出后仍停留在 WSL shell

### 冒烟记录
- 临时验证脚本：`.\test-codex-tab.ps1`
- 结果：
  - codex 可在 WSL 中正常启动
  - tab 外观与 PowerShell tab 保持一致
  - 不再出现 `wt -l` 参数报错弹窗

---

## 2026-03-16 v1.3

### 范围
- 调整 `start-dev.ps1` 的 codex tab：不再进入 WSL，直接在 Windows（PowerShell）环境启动 `codex`。

### 背景
- 现状：codex tab 目前通过 `pwsh` 作为入口，再进入 WSL 启动 `codex`。
- 需求变化：后续不再需要 WSL，计划改为 Windows 环境直启。

### 目标
- codex tab 行为：直接执行 `codex`（不附加额外参数）。
- 若 Windows 环境找不到 `codex` 命令：
  - tab 不退出（保持 PowerShell 交互）
  - 给出明确提示“未找到命令：codex…请确认已安装并加入 PATH”
- 同步移除对 `wsl` 的硬依赖检查（避免未安装 WSL 时脚本直接报错）。

### 验收点
- `.\start-dev.ps1 -DryRun` 输出参数中不再包含 `wsl.exe`/WSL 相关命令。
- `.\start-dev.ps1`：
  - 若已安装 `codex`：codex tab 能正常启动 `codex`；
  - 若未安装 `codex`：codex tab 提示缺失且保持在 PowerShell，不会闪退/自动关闭。

---

## 2026-03-28 v1.4

### 背景
- 项目开发环境迁移到 WSL：前后端进程都应在 WSL 内启动，Windows 仅作为浏览器/IDE 使用。
- 现有 `start-dev.ps1` 依赖 Windows 服务版 MongoDB；WSL 内默认无 `mongod/mongosh`，需要新的数据库启动方案。

### 目标
- 在 WSL 内一条命令拉起开发环境（数据库/缓存/后端/前端），并可实时查看各自日志。
- 进程托管采用 `tmux`，在单个终端内模拟“多 tab”体验（多个 window/pane 分流日志）。
- 默认访问：
  - 前端：`http://localhost:5777`（Windows 浏览器直接访问）
  - 后端：`http://localhost:3000/api/health`
- 若 Windows 侧无法访问 WSL 内前端地址，提供兜底：Vite 以 `--host 0.0.0.0` 监听全部网卡。

### 设计方案
#### 1) 数据库/缓存：Docker（WSL 内）
- 新增 `docker/mongo/compose.yml`
  - 镜像：`mongo:7`
  - 容器名：`lms-mongo`
  - 端口：`127.0.0.1:27017:27017`（仅绑定本机，避免局域网暴露）
  - 数据卷：`lms-mongo-data:/data/db`
  - `restart: unless-stopped`
- Redis 继续使用现有 `docker/redis/compose.yml`（容器名 `lms-redis`）。
- 后端默认连接串保持不变：`MONGODB_URI=mongodb://127.0.0.1:27017`（容器端口映射到宿主机本机回环后即可直连）。

#### 2) 启动脚本：WSL + tmux
- 新增 `start-dev.sh`（仅在 WSL 内运行）
  - 依赖检查：`docker`、`tmux`、`pnpm`
  - 拉起基础设施：
    - `docker compose -f docker/mongo/compose.yml up -d`
    - `docker compose -f docker/redis/compose.yml up -d`
  - 创建/复用 `tmux` session（建议：`lms-dev`），并在 session 内启动：
    - 后端：`pnpm -C backend dev`
    - 前端：`node --no-turbo-inlining .../vite.js --mode development --port <VITE_PORT>`（必要时支持追加 `--host 0.0.0.0`）
      - 备注：在部分环境中 `pnpm dev:ele` 触发 V8 fatal error（SIGILL），脚本使用 `--no-turbo-inlining` 作为兜底，避免开发服务器异常退出。
  - 默认行为：脚本执行后自动 `tmux attach -t lms-dev`，方便立即看日志。
  - 额外参数：
    - `--host-all`：等价于前端追加 `--host 0.0.0.0`
    - `--no-attach`：启动后不自动 attach（便于脚本化/自检）
- 停止脚本暂不作为本次范围（可先手动：`tmux kill-session -t lms-dev`）。

#### 3) Windows MongoDB → WSL Docker MongoDB 迁移（手工步骤）
- Windows 导出（在 Windows 侧执行）：
  - `mongodump --uri="mongodb://127.0.0.1:27017" --db=library --out=<dump_dir>`
- 将 `<dump_dir>` 放到 WSL 可访问路径（例如：`/mnt/c/...`）。
- WSL 导入（Mongo 容器已启动后）：
  - `mongorestore --uri="mongodb://127.0.0.1:27017" --db=library --drop <dump_dir>/library`
  - 说明：`--drop` 会先清空目标库再导入；如需保留现有数据可去掉该参数。
  - 若 WSL 未安装 `mongorestore`，可直接用 Docker：
    - `docker run --rm --network container:lms-mongo -v <dump_dir>:/dump mongo:7 mongorestore --db=library --drop /dump/library`

### 验收点
- WSL 执行 `./start-dev.sh` 后：
  - `docker ps` 能看到 `lms-mongo`、`lms-redis` 为 Up（Redis `ping` 为 `PONG`）。
  - `curl http://localhost:3000/api/health` 返回成功。
  - Windows 浏览器可打开 `http://localhost:5777` 正常访问页面。
- 二次执行 `./start-dev.sh` 行为幂等：复用/提示已有 `tmux` session，不重复拉起多份前后端进程。

### 冒烟记录
- `2026-03-28`：
  - `chmod +x ./start-dev.sh`
  - `./start-dev.sh --no-attach`
  - 验证：
    - `docker ps`：`lms-mongo`、`lms-redis` 为 Up
    - `docker exec lms-redis redis-cli ping`：`PONG`
    - `docker exec lms-mongo mongosh --quiet --eval 'db.adminCommand({ ping: 1 })'`：`{ ok: 1 }`
    - `tmux list-windows -t lms-dev`：存在 `mongo/redis/backend/frontend` 四个 window
    - `curl http://127.0.0.1:3000/api/health`：返回 `{ code: 0, ... db.ok: true }`
    - `curl http://127.0.0.1:5777`：返回 HTML（`<!doctype html>`）
  - 幂等验证：再次执行 `./start-dev.sh --no-attach`，提示 session 已存在，不会重复拉起前后端进程。

---

## 2026-03-28 v1.5

### 范围
- 新增停止脚本 `stop-dev.sh`（WSL 内运行）：一条命令关闭由 `start-dev.sh` 拉起的全部服务。
- 同步更新 `RUNNING.md`：停止方式改为 `./stop-dev.sh`（不再要求手动 `tmux kill-session` + `docker compose down`）。

### 目标
- 最简使用：`./stop-dev.sh`（无参数）。
- 默认关闭范围：
  - 前端/后端（通过关闭 `tmux` session：`lms-dev`）
  - Redis（`docker compose -f docker/redis/compose.yml down`，不带 `-v`，保留数据卷）
  - MongoDB（`docker compose -f docker/mongo/compose.yml down`，不带 `-v`，保留数据卷）
- 幂等：重复执行不会报错（服务不存在/已停止时尽力而为）。

### 设计要点
- 执行顺序：先停容器、再杀 tmux session（避免在 tmux 内执行时被中途 kill 掉，导致脚本没跑完）。
- 不清理数据卷（避免误删迁移后的 Mongo/Redis 数据）；如需清理数据卷后续再另起版本讨论。

### 验收点
- WSL 执行 `./stop-dev.sh` 后：
  - `tmux has-session -t lms-dev` 不存在
  - `docker ps` 不包含 `lms-mongo`、`lms-redis`
  - 二次执行仍正常退出且无致命错误

### 冒烟记录
- `2026-03-28`：
  - 启动：`./start-dev.sh --no-attach`
  - 停止：`./stop-dev.sh`
  - 验证：
    - `tmux has-session -t lms-dev`：停止后不存在
    - `docker ps`：停止后不包含 `lms-mongo`、`lms-redis`
    - 幂等：再次执行 `./stop-dev.sh` 仍可正常退出（无致命错误）
