# 图书管理开发记录

## 适用范围
- 页面：`vue-vben-admin/apps/web-ele/src/views/library/books/index.vue`
- 接口：`GET /api/books`
- 相关文件：
  - `backend/src/routes/books.ts`
  - `backend/API_CONTRACT.md`
  - `vue-vben-admin/apps/web-ele/src/api/library/books.ts`

## 目录
- `2026-03-15 v1.0`：图书管理列表表头排序开发计划
- `2026-03-15 v1.1`：恢复表头三态排序
- `2026-03-15 v1.2`：重置/刷新统一恢复默认排序
- `2026-03-19 v1.3`：下架改为抽屉确认 + 修复重置后仍用旧筛选查询
- `2026-03-21 v1.4`：已下架图书物理删除
- `2026-03-21 v1.5`：上/下架接口参数类型校验
- `2026-03-21 v1.6`：下架增加未结束记录/库存未回收校验
- `2026-03-22 v1.7`：移除导入自动上架 + 上架需有库存 + 记录上下架人/时间
- `2026-03-22 v1.8`：新书入库默认下架（手动新建/批量导入一致）+ mock 校验对齐

---

## 2026-03-15 v1.0

### 范围
- 图书管理列表新增表头排序
- 默认保持“入库时间倒序”
- 支持“当前可借”排序

### 页面改动点
- 表头开启远程排序：
  - `当前可借`
  - `入库时间`
- 默认排序：
  - `入库时间 desc`

### 接口改动点
- `GET /api/books` 扩展排序参数能力：
  - `sortBy?: 'created_at' | 'current_stock'`
  - `sortOrder?: 'asc' | 'desc'`
- 排序规则：
  - `created_at`：标准升序/降序
  - `current_stock`：标准升序/降序
- `current_stock` 排序下，同库存值按 `created_at desc`

### 实现约束
- 搜索区不新增排序表单
- “重置”仅处理筛选项
- “刷新”保留当前排序
- 查询成功后需手动回显表头排序激活状态

### 验收点
- 页面初次进入默认按入库时间倒序
- 点击“入库时间”列头可切换升序/降序
- 点击“当前可借”列头可切换升序/降序
- 点击“重置”后，当前排序仍保持
- 点击工具栏“刷新”后，当前排序仍保持

### 自检记录
- `2026-03-15`
- `pnpm -C backend build`：通过
- `pnpm -C vue-vben-admin -F @vben/web-ele typecheck`：通过
- 接口冒烟：通过
  - `POST /api/auth/login` 使用内置账号 `vben / 123456` 获取 token：成功
  - `GET /api/books?page=1&pageSize=10&status=all`：默认仍为 `created_at desc`
  - `GET /api/books?...&sortBy=current_stock&sortOrder=asc`：通过
  - `GET /api/books?...&sortBy=current_stock&sortOrder=desc`：通过
  - `current_stock` 相同值下按 `created_at desc` 校验：通过

## 2026-03-15 v1.1

### 调整原因
- 手动回显排序状态后，图书管理表头无法通过第三次点击清空排序

### 改动点
- 排序状态调整为 `active + field + order`
- 用户清空排序后，保留默认请求排序，但表头不再强制显示默认激活状态
- `重置`、`刷新` 在当前有排序或无排序两种状态下都保持一致

### 验收点
- 表头支持 `asc -> desc -> 无排序` 三态切换
- 清空排序后，表头保持无激活状态
- `重置`、`刷新` 不破坏当前排序或清空状态

## 2026-03-15 v1.2

### 调整原因
- 实际交互中，“无排序表头 + 默认顺序数据”容易造成理解混乱
- 为降低心智负担，图书管理改回更简单、可预期的默认行为

### 改动点
- 删除额外的排序状态保留逻辑
- 以下操作统一恢复默认排序并回显默认表头激活态：
  - 点击表头第三次清空排序
  - 点击“重置”
  - 点击工具栏“刷新”
- 默认排序：
  - `入库时间 desc`

### 验收点
- 表头点击升序/降序正常
- 第三次点击清空排序后，恢复默认排序 `入库时间 desc`
- 点击“重置”后，筛选与排序都恢复默认
- 点击工具栏“刷新”后，排序恢复默认

## 2026-03-19 v1.3

### 调整原因
- 图书管理“下架”希望改为抽屉确认（信息可见、可读、可确认），交互与“还书”等写操作保持一致。
- 修复：点击搜索区“重置”后，表单字段虽已清空，但后续查询仍使用旧筛选值（导致表格数据不更新/仍为空）。

### 范围
- 页面：`vue-vben-admin/apps/web-ele/src/views/library/books/index.vue`
- 下架接口：`PUT /api/books/:isbn/shelf`（`{ "is_deleted": true }`）
- 说明：重置逻辑为多页面复用，本次修复同时覆盖：
  - 管理员端：图书管理 / 借阅管理 / 用户管理
  - 用户端：图书预约 / 借阅记录（如存在同类重置入口）

### 页面改动点
- “下架”按钮改为打开抽屉：
  - 标题：`下架图书`
  - 内容：与“编辑图书”抽屉表单一致，但全部只读
  - 底部按钮：`确认下架`、`取消`
- `取消`：
  - 清理抽屉表单临时数据（恢复为未打开状态）
  - 关闭抽屉
- `确认下架`：
  - 弹出二次确认（标题：`确认下架`；内容沿用现有下架确认文案）
  - 确认后调用下架接口，成功后刷新列表

### 验收点
- 点击“下架”后出现抽屉，字段只读且与“编辑图书”展示一致
- 点击“取消”后抽屉关闭，且不会残留上一次打开的临时数据
- 点击“确认下架”会先弹出二次确认，确认后列表状态刷新
- 点击“重置”后再次查询使用重置后的表单条件（不再沿用旧筛选值）

### 自检记录
- `2026-03-19`
- `pnpm -C vue-vben-admin -F @vben/web-ele typecheck`：通过

## 2026-03-21 v1.4

### 范围
- 页面：`vue-vben-admin/apps/web-ele/src/views/library/books/index.vue`
- 新增接口：`DELETE /api/books/:isbn`
- 相关文件：
  - `backend/src/routes/books.ts`
  - `backend/API_CONTRACT.md`
  - `vue-vben-admin/apps/web-ele/src/api/library/books.ts`

### 需求/规则
- 操作栏新增“删除”按钮：仅已下架（`is_deleted=true`）的图书行显示
- 删除交互：抽屉（只读展示，复用“编辑图书”字段）+ 抽屉确认时二次确认弹框
- 权限：仅 `admin | super`
- 后端约束：未下架禁止删除（返回 `409`）

### 验收点
- 正常状态不出现“删除”
- 已下架出现“删除”，删除成功后列表刷新且该条消失
- 非 `admin/super` 调用删除接口返回 `403`

### 自检记录
- `2026-03-21`
- `pnpm -C backend build`：通过
- `pnpm -C vue-vben-admin -F @vben/web-ele typecheck`：通过

### 冒烟记录
- `2026-03-21`：通过
  - 启动后端：`node backend/dist/server.js`（默认 `http://127.0.0.1:3000`）
  - 登录：`POST /api/auth/login`（`admin / 123456`）获取 `accessToken`：成功
  - 新增测试书：`POST /api/books`：成功
  - 下架：`PUT /api/books/:isbn/shelf`（`{ "is_deleted": true }`）：成功
  - 删除：`DELETE /api/books/:isbn`：成功
  - 删除后查询：`GET /api/books/:isbn`：`404`（符合预期）

## 2026-03-21 v1.5

### 范围
- 接口：`PUT /api/books/:isbn/shelf`
- 相关文件：
  - `backend/src/routes/books.ts`
  - `backend/API_CONTRACT.md`

### 需求/规则
- 修复 `is_deleted` 未做类型校验导致的误判：
  - 仅接受 `boolean`（`true | false`）
  - `is_deleted` 缺失或非 `boolean`：返回 `400`
- 兼容性说明：不支持 `"false" | "0" | 0 | 1` 等“伪布尔”输入（直接按参数不合法处理）

### 验收点
- `PUT /api/books/:isbn/shelf` 传 `{ "is_deleted": true }`：下架成功
- `PUT /api/books/:isbn/shelf` 传 `{ "is_deleted": false }`：上架成功
- `PUT /api/books/:isbn/shelf` 传 `{ "is_deleted": "false" }`：`400`
- `PUT /api/books/:isbn/shelf` 不传 `is_deleted`：`400`

## 2026-03-21 v1.6

### 范围
- 接口：`PUT /api/books/:isbn/shelf`
- 相关文件：
  - `backend/src/routes/books.ts`
  - `backend/API_CONTRACT.md`

### 需求/规则（保守版）
- 下架（`is_deleted=true`）增加后端业务校验：
  - 若该 ISBN 存在“未结束”的借阅/预约记录：返回 `409`，禁止下架
  - “未结束”定义：借阅记录 `status` 属于 `reserved/reserve_overdue/borrowed/borrow_overdue`（可选兼容历史状态 `overdue`）
  - 错误文案需区分原因，且尽可能可定位：
    - 至少包含“未结束预约数/未结束借阅数”（如：`存在未结束借阅记录（2）/未结束预约记录（1），禁止下架`）
  - 额外兜底：若 `current_stock < total_stock`，返回 `409` 禁止下架（避免“库存未回收但记录已被清空/不一致”的异常）
- 上架（`is_deleted=false`）不增加额外限制
- 说明：还书接口本身不依赖 `is_deleted`（用于兼容历史/脏数据，避免出现“无法归还”的死锁记录）

### 验收点
- 存在未结束记录时，下架返回 `409`（错误文案需可读、可定位原因）
- 不存在未结束记录时，下架成功，列表状态刷新为“已下架”
- 已下架图书依旧禁止新增借阅/预约（已有逻辑保持 `409`）

### 自检记录
- `2026-03-21`
- `pnpm -C backend build`：通过

### 冒烟记录
- `2026-03-21`：通过
  - 启动后端：`node backend/dist/server.js`（默认 `http://127.0.0.1:3000`）
  - 登录：`POST /api/auth/login`（`admin / 123456`）获取 `accessToken`：成功
  - 创建临时用户：`POST /api/users`：成功
  - Case1：存在未结束借阅记录时下架：返回 `409`，文案包含未结束借阅/预约数量：通过
  - Case2：库存未回收（`current_stock < total_stock`）下架：返回 `409`，文案包含 `库存未回收（x/y）`：通过
  - Case3：无未结束记录且库存已回收下架：返回 `200`：通过

## 2026-03-22 v1.7

### 范围
- 页面：`vue-vben-admin/apps/web-ele/src/views/library/books/index.vue`
- 接口：
  - `PUT /api/books/:isbn/shelf`
  - `POST /api/books/import/commit`
- 相关文件：
  - `backend/src/routes/books.ts`
  - `backend/src/routes/books-import.ts`
  - `backend/src/db/collections.ts`
  - `backend/API_CONTRACT.md`
  - `vue-vben-admin/apps/web-ele/src/api/library/books.ts`

### 需求/规则
- 上/下架必须人工控制：
  - 批量导入“命中老书 + 叠加库存”不再触发自动上架（不再修改 `is_deleted`）
  - 前端移除导入里的“自动上架”开关
- 上架必须有库存：
  - 上架（`is_deleted=false`）时要求 `current_stock > 0`，否则返回 `409`
  - 说明：下架已限制“库存未回收”，因此正常数据下下架状态不会出现“借出导致 current_stock 为 0”的情况；本规则主要用于兜底防止“无可借库存的书被上架”
- 记录上下架人/时间（仅保留最近一次）：
  - 上架：写入“上架人/上架时间”
  - 下架：写入“下架人/下架时间”
  - 表格默认不展示（避免挤占业务表格空间）
- 交互提示：
  - 成功/失败都必须有明确提示，文案尽量短且能定位原因

### 接口改动点
- `PUT /api/books/:isbn/shelf`
  - 上架校验：`current_stock === 0` 返回 `409`
  - 上/下架成功时写入对应“操作人/操作时间”（仅最近一次）
- `POST /api/books/import/commit`
  - Body 移除 `auto_unshelf`
  - 行为调整：导入叠加库存仅更新库存字段，不修改 `is_deleted`

### 页面改动点
- 批量导入：
  - 移除“自动上架”开关与提交参数
- 上/下架：
  - 接口失败时提示文案需明确（例如：`无库存，禁止上架` / `权限不足` 等）
  - （可选）在只读抽屉中展示最近一次上下架信息（不进入列表表格）

### 验收点
- 导入命中已下架图书时：库存叠加成功，但图书仍保持“已下架”
- 上架可借库存为 0（`current_stock=0`）的图书：返回 `409`，文案简单明确
- 上/下架成功后：能在图书记录中查到最近一次上/下架人及时间

### 自检记录
- `2026-03-22`
- `pnpm -C backend build`：通过
- `pnpm -C vue-vben-admin -F @vben/web-ele typecheck`：通过

### 冒烟记录
- `2026-03-22`：通过
  - 启动后端：`node backend/dist/server.js`（默认 `http://127.0.0.1:3000`）
  - 登录：`POST /api/auth/login`（`admin / 123456`）获取 `accessToken`：成功
  - Case1：下架后批量导入命中老书（累加库存）：库存叠加成功且仍保持“已下架”（未自动上架）：通过
  - Case2：下架书手动改为 `current_stock=0` 后尝试上架：返回 `409`（无可借库存，禁止上架）：通过
  - Case3：上/下架成功后，列表查询能看到最近一次 `shelved_*` / `unshelved_*` 字段被写入：通过
  - 清理：删除测试书：通过

## 2026-03-22 v1.8

### 范围
- 真实后端：
  - `POST /api/books`（手动新书入库）
  - `POST /api/books/import/commit`（批量导入提交：新书分支）
  - `PUT /api/books/:isbn/shelf`（上/下架校验保持：上架需有库存、is_deleted 类型校验）
- mock（用于前端 mock 模式行为对齐）：
  - `POST /api/books`
  - `POST /api/books/import/commit`（新书分支）
  - `PUT /api/books/:isbn/shelf`

### 需求/规则
- 新书入库默认下架（必须手动上架）：
  - 手动新建新书：默认 `is_deleted=true`，不自动写入 `shelved_*`
  - 批量导入新书：默认 `is_deleted=true`，不自动写入 `shelved_*`
- 批量导入老书：
  - 仅增加库存（`total_stock/current_stock` + delta）
  - 不改变该书当前 `is_deleted`（保持上/下架状态不变）
- mock 对齐后端接口关键校验：
  - `PUT /api/books/:isbn/shelf`：`is_deleted` 必须为 boolean，否则 `400`
  - 上架（`is_deleted=false`）要求 `current_stock > 0`，否则 `409`

### 验收点
- Case1：手动新建一本书 -> 列表状态为“已下架”，用户端预约列表不可见 -> 管理端手动“上架”后用户端可见并可预约
- Case2：批量导入一条新书 -> 同 Case1（默认下架，需手动上架）
- Case3：批量导入命中老书（increment/overwrite）-> 仅库存变化，`is_deleted` 不变
- Case4（mock）：上架传非 boolean -> `400`；上架时 `current_stock<=0` -> `409`

### 自检记录（待补）
- `2026-03-22`
- `pnpm -C backend build`：通过
- `pnpm -C vue-vben-admin -F @vben/web-ele typecheck`：通过

### 冒烟记录（待补）
- `2026-03-22`：通过（后端接口冒烟）
  - 启动后端（临时端口避免冲突）：`PORT=3100 node backend/dist/server.js`
  - 登录：`POST /api/auth/login`（`admin / 123456`）获取 `accessToken`：成功
  - 手动新建：`POST /api/books`：返回 `is_deleted=true`（默认下架）：通过
  - 手动上架：`PUT /api/books/:isbn/shelf`（`{ "is_deleted": false }`）：通过
  - 导入新书：`POST /api/books/import/preview` + `POST /api/books/import/commit`：新书出现在 `status=deleted` 列表：通过
  - 停止后端：结束 `node` 进程：通过
